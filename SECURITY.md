# 보안 정책

## 지원 버전

cloudloupe는 1.0 이전입니다. 보안 수정은 최신 마이너 릴리스에만 반영됩니다. 신고 전에
업그레이드해주세요.

| 버전     | 지원 |
| -------- | ---- |
| 최신     | O    |
| 그 이전  | X    |

## 취약점 신고

**보안 문제를 공개 이슈로 열지 말아주세요.**

[GitHub Security Advisories](https://github.com/cnlgks1/cloudloupe/security/advisories/new)를
통해 비공개로 신고해주세요. 이 방법을 쓸 수 없다면, 비공개 연락 경로를 요청하는 내용만
담아서 이슈를 열어주세요. 상세 내용은 넣지 마세요.

다음 내용을 포함해주세요.

- 문제 설명과 그것이 왜 보안 사안인지
- 재현 절차 또는 PoC
- 영향받는 버전과 플랫폼
- 제안하는 수정 방향(있다면)

7일 내에 접수 확인, 30일 내에 진행 상황을 알려드립니다. 수정이 릴리스되면 원하지 않는다고
하시지 않는 한 어드바이저리에 기여자로 기록합니다.

## 대상 범위

cloudloupe는 AWS 자격증명과 인프라 메타데이터를 읽으므로, 보안 표면은 좁지만 실재합니다.

범위에 포함되는 것:

- **자격증명 노출.** 자격증명, 세션 토큰, SSO 캐시 내용이 로그, 리포트, 스냅샷 캐시, 에러
  메시지, 터미널 스크롤백으로 새는 모든 경우.
- **AWS로 향하는 쓰기 경로.** cloudloupe는 설계상 조회 전용입니다. AWS 리소스를 생성,
  수정, 삭제할 수 있는 코드 경로는 그 자체로 보안 버그이며, 심각한 버그입니다.
  `verify-readonly` allow-list를 우회하는 방법도 여기 포함됩니다.
- **AWS 엔드포인트 외부로 나가는 네트워크 트래픽.** cloudloupe에는 텔레메트리가 없고 어떤
  제3자 서비스와도 통신하지 않습니다. 다른 곳으로 가는 트래픽은 버그입니다.
- **로컬 권한 및 경로 문제.** 스냅샷 캐시나 리포트 파일을 과도하게 열린 권한으로 쓰는 경우,
  사용자가 지정한 출력 경로를 통한 경로 탈출, `~/.aws` 파일 내용의 안전하지 않은 처리.
- **공급망.** 침해되었거나 타이포스쿼팅된 의존성, 또는 소스 태그와 일치하지 않는 릴리스
  산출물.

범위에 포함되지 않는 것:

- 공격자가 이미 사용자의 기계나 `~/.aws` 파일을 장악한 상태를 전제하는 발견.
- IAM 권한 부족으로 혼란스러운 에러가 나는 경우. UX 문제이므로 일반 버그로 신고해주세요.
- AWS 자체의 취약점.
  [AWS Security](https://aws.amazon.com/security/vulnerability-reporting/)로 신고하세요.

## cloudloupe가 자격증명으로 하는 일

여기서 벗어나는 동작을 발견하기 쉽도록 명확히 적어둡니다.

- 자격증명은 AWS SDK for Go v2가 표준 체인으로 해석합니다. cloudloupe는 비밀값을 직접
  파싱하는 코드를 갖고 있지 않습니다.
- `~/.aws/config`와 `~/.aws/credentials`는 프로필 이름과 기본 리전을 파악하기 위해
  **읽기만** 합니다. 쓰거나 수정하지 않습니다.
- 자격증명은 프로세스가 살아 있는 동안만 메모리에 있고, 스냅샷 캐시에 기록되지 않으며,
  리포트에 포함되지 않습니다.
- 선택한 프로필과 리전의 AWS API 엔드포인트 외에 어디로도 데이터를 보내지 않습니다.

## 릴리스 검증

모든 릴리스에는 `checksums.txt`가 함께 배포됩니다. 바이너리를 실행하기 전에 확인하세요.

```sh
sha256sum -c checksums.txt --ignore-missing
```
