# 보안 정책

## 지원 버전

최신 릴리스에만 보안 수정을 제공합니다. 문제를 확인하면 패치 버전을 새로 게시합니다.

## 취약점 신고

취약점은 공개 이슈로 열지 말고 비공개로 알려 주세요. GitHub의
[Security Advisories](https://github.com/cnlgks1/cloudloupe/security/advisories/new)로 신고하면
됩니다. 수정과 공개 준비가 끝날 때까지 내용은 비공개로 유지됩니다.

신고에 아래를 담아 주시면 확인이 빠릅니다.

- 문제가 나타나는 버전(`cloudloupe --version`)과 운영체제
- 재현 절차
- 예상 동작과 실제 동작
- 영향 범위에 대한 판단(있으면)

## 이 도구의 보안 설계

cloudloupe는 조회 전용이며, 다음을 설계로 보장합니다. 이 경계를 벗어나는 동작은 그 자체로
취약점입니다.

- **쓰기 없음.** AWS SDK 호출은 `Describe`, `List`, `Get`, `Lookup`, `Search`, `BatchGet`
  접두사만 허용하고, CI의 `make verify-readonly` 가드가 그 외 호출을 막습니다. 수집기
  인터페이스에는 쓰기 경로 자체가 없습니다.
- **자격증명은 읽기만.** `~/.aws`의 설정과 자격증명 파일을 파싱만 하고 수정하지 않습니다.
- **비밀 값 미조회.** Secrets Manager 시크릿과 SSM 파라미터는 메타데이터만 읽고, 값을
  가져오는 `GetSecretValue`·`GetParameter`는 호출하지 않습니다.
- **외부 전송 없음.** 수집한 데이터를 외부로 보내지 않고 화면에만 표시합니다.

따라서 특히 다음에 해당하면 취약점으로 신고해 주세요.

- 조회 전용 경계를 우회해 리소스를 변경할 수 있는 경로
- 자격증명·시크릿·토큰 같은 민감한 값이 화면, 로그, 리포트에 노출되는 문제
- 설정 파일 처리에서 발생하는 경로 순회나 임의 파일 접근

## 신고자 안전

이 저장소의 예제·테스트·데모 데이터는 모두 AWS 문서의 예제 계정(`123456789012` 등)과
지어낸 값만 씁니다. 재현 절차를 보낼 때 실제 계정 ID, ARN, 리소스 이름 같은 정보는 예제 값으로
바꿔 주세요. 신고 내용에 그런 정보가 필요하지 않습니다.
