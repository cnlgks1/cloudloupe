module github.com/cnlgks1/cloudloupe

// Floor is set by github.com/charmbracelet/bubbles, which requires go >= 1.24.2.
// Keep this at a minor+patch floor, never pinned to the local toolchain's exact
// patch version, or every contributor is forced onto that same build.
go 1.24.2
